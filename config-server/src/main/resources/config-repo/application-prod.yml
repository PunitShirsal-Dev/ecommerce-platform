# Use with SPRING_PROFILES_ACTIVE=prod on Kubernetes / production.
spring:
  thymeleaf:
    cache: true

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    health:
      probes:
        enabled: true
